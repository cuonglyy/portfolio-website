import { defineStyleConfig } from '@chakra-ui/react'

const Divider = defineStyleConfig({
  baseStyle: {
    w: '85%',
    my: ['75px', '85px', '85px'],
    display: 'block'
  }

});

export default Divider